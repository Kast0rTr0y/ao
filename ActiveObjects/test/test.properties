# JDBC
jdbc.url = ${jdbc.url}
jdbc.username = ${jdbc.username}
jdbc.password = ${jdbc.password}

# Parameters, see net.java.ao.test.config.ParametersLoader for more information
test.parameters = ${test.parameters}